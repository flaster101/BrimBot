# Research and environment log

## 2026-09-15

- Empty local Git repository inspected; no existing application or datasets.
- GitHub connector authenticated successfully as `flaster101`.
- Supplied YouTube short URL failed in web extraction. Direct public oEmbed
  succeeded after network permission and returned title and author metadata.
- Located official SYBO help pages and cross-checked community enemy taxonomy.
- Local environment initially has Java 8 runtime only; no Android SDK, adb,
  Gradle, PyTorch or TensorFlow. Installing isolated build and analysis tools.
- No live Android gameplay has been observed. No training or gameplay quality
  claim follows from successful dependency setup.

## 2026-09-16

- Downloaded three attributed public sources locally. No raw game footage or
  extracted-frame dataset is redistributed.
- Corrected temporal-budget sampling; sampled 702 train/validation frames.
- A full contact-sheet audit found that the original validation region mostly
  showed chest UI. Refined the limited surface experiment's train/validation
  times before fitting; retained the original final test quarantine.
- CLIPSeg first prompt pair accepted 0/60 frames. A ground/grass experiment
  accepted 5/80. Narrowed candidate coverage explicitly to grass, retained
  optical-flow consistency and pixel uncertainty; final accepted split 30/19.
- Fitted and compared two students. Selected the custom CNN using validation
  loss; exported to ONNX and checked numerical parity. This is a surface
  appearance experiment, not a safe-navigation model.
- Started independent-source replay only after model selection. No checkpoint
  or threshold is selected using this source. Replay cannot measure survival.
